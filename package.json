{
  "name": "uxml-workspace",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "ts-node": "^10.0.0"
  },
  "workspaces": [
    "uxml",
    "uxml-benchmark"
  ],
  "scripts": {
    "uxml:build": "yarn workspace uxml build",
    "uxml:test": "yarn workspace uxml test",
    "benchmark:simple": "yarn workspace uxml-benchmark run-simple"
  }
}
